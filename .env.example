# Copy to .env and adjust only for local development against `next dev`.
# Production builds default to https://nexusdigitallabs.dev — see src/lib/config.ts.
EXPO_PUBLIC_API_BASE_URL=http://localhost:3000
